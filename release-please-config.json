{
  "$schema": "https://raw.githubusercontent.com/googleapis/release-please/main/schemas/config.json",
  "release-type": "node",
  "include-v-in-tag": true,
  "separate-pull-requests": false,

  "//1": "Single package, so the tag is plain vX.Y.Z. The sibling engine and rgs",
  "//2": "repos prefix theirs (studio-front/v1.2.3) because they are monorepos",
  "//3": "releasing several services from one history; there is nothing here to",
  "//4": "disambiguate. Left at the default this would have been `ui-v1.0.0`,",
  "//5": "with a hyphen, which is neither shape.",
  "include-component-in-tag": false,

  "//6": "Breaking changes bump the MINOR while under 1.0.0. Without this a `!`",
  "//7": "commit takes 0.1.0 straight to 1.0.0 — which is what the first release",
  "//8": "PR proposed. Cutting 1.0.0 means promising semver on a token layer that",
  "//9": "is still moving. Delete this flag when the system settles; that is the",
  "//10": "deliberate act that makes 1.0.0 mean something.",
  "bump-minor-pre-major": true,

  "packages": {
    ".": {
      "component": "ui",
      "package-name": "@engineio/ui",
      "changelog-path": "CHANGELOG.md"
    }
  },
  "changelog-sections": [
    { "type": "feat", "section": "Features" },
    { "type": "fix", "section": "Bug Fixes" },
    { "type": "tokens", "section": "Tokens" },
    { "type": "perf", "section": "Performance" },
    { "type": "docs", "section": "Documentation", "hidden": false },
    { "type": "refactor", "section": "Refactors", "hidden": false },
    { "type": "style", "section": "Styling", "hidden": false },
    { "type": "chore", "section": "Miscellaneous Chores", "hidden": true }
  ]
}
